# Copy to backend/.env and edit. Never commit .env.

# mock (default, no key needed) | anthropic
LLM_PROVIDER=mock

# Only needed when LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5
